import { SyncLoader } from "react-spinners"
import "./style.css"

const Loader = () =>{
    return(
        <div className="loader">
            <SyncLoader/>
        </div>
    )
}

export default Loader